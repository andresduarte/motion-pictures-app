<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Films</title>
    <link rel="stylesheet" type="text/css" href="/style.css">
  </head>
  <body>

    <h2> Films </h2>

      <% @films.each do |film| %>

      <ul>

        <li><h3><a href="films/<%= film.slug %>"><%= film.title.capitalize %></a></h3></li>
        <li class="tab"><h4>genre: <%= film.genre %></h4></li>
        <li class="tab"><h4>runtime: <%= film.runtime + " min"%></h4></li>

      </ul>

      <% end %>

      <% if logged_in? %>

        <a href="/users/<%= current_user.slug %>">Profile</a><br>
        <a href="/films/new">New Film</a><br>
        
      <% end %>

  </body>
</html>
